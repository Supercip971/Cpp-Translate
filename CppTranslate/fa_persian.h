#pragma once
// c++ در فارسی
// فقط برای سرگرمی

#include <string>
#include <list>
#include <cstring>
#include <vector>
#define سی‌پلاس‌پلاس‌ـ‌فارسی
#pragma region دیگر
#define استفاده‌کن using
#define را
#define اصلی main

#define ــ‌خط‌ــ __LINE__
#define ــ‌فایل‌ــ __FILE__
#pragma endregion

#pragma region اپراتور

#define اسم‌نوع typename
#define قالب template
#define اندازه‌ی sizeof
#define اپراتور operator

#define کلاس class
#define ساختار struct
#define شمارش enum

#define ثابت const
#define مجازی virtual
#define ساکن static
#define درخط inline

#define عمومی public
#define خصوصی private
#define محفوظ protected

#define شناسه‌نوع typeid

#define پرتاب‌کن throw
#define برگردان return

#define جدید new
#define تازه new
#define نو new
#define حذف delete
#define حذف‌آرایه delete[]

#define یا or
#define و and
#define نه not

#define درست true
#define نادرست false

#define بعلاوه‌ی +
#define می‌شود =
#define برابر ==
#define ضرب‌در *
#define منها -
#define منفی -
#define تقسیم /

#define مرجع(A) (&A)
#define اشاره‌گر(A) A*
#define فرزند :
#define اندیس(A) [A]

#define بزرگ‌تر‌از >
#define کوچک‌تر‌از <
#define بزرگ‌تر‌یا‌مساوی‌با >=
#define کوچک‌تر‌یا‌مساوی‌با <=

#define درست‌است == درست
#define نادرست‌است == نادرست

#pragma endregion

#pragma region نوع

استفاده‌کن خالی = void;

استفاده‌کن بولی = bool;
استفاده‌کن کارکتر = char;
استفاده‌کن صحیح = int;
استفاده‌کن حسابی = unsigned int;
استفاده‌کن شناور = float;
استفاده‌کن دو‌شناور = double;
استفاده‌کن رشته = std::string;

استفاده‌کن رشته‌کارکترهای‌سی‌که‌باصفرختم‌می‌شود = char*;

قالب<کلاس ت>
استفاده‌کن بردار = std::vector<ت>;

قالب<کلاس ت>
استفاده‌کن لیست = std::list<ت>;

#pragma endregion
#pragma region حلقه یا دیگر

#define اگر if
#define دیگر else
#define دیگر‌اگر else if
#define تکرار‌کن while
#define برای for
#define برای‌هر for each
#pragma endregion

#pragma region عملکرد‌ها
#define بنویس printf

#pragma endregion

#define کپی‌حافظه memcpy
#define تنظیم‌حافظه memset
#define اندازه‌ی‌یک‌رشته‌کارکترسی strlen
#define کپی‌رشته‌کارکتر strcpy
